---
extends: default

yaml-files:
  - "*.yaml"
  - "*.yml"
  - ".yamllint"

rules:
  comments-indentation: disable
  document-start: disable
  line-length: disable
  new-lines: disable
  truthy: disable
